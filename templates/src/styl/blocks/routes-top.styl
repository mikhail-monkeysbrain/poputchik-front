.routes
  &-top
    width 100%
    height 60rem
    margin-top -2rem
    background url(../images/content/route-page.jpg) center top no-repeat
    background-size 100%
    position relative
    &--container
      display flex
      flex-direction: column
      height: 60rem
      justify-content: center
    &--heading
      color #ffffff